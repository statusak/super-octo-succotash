# ASP.NET Application: Event Manager

Пример веб‑приложения на ASP.NET для управления мероприятиями с RESTful API и документацией Swagger.

## Функциональность

В приложении реализованы:

1. **Модель `Event`** — представляет данные о мероприятии со следующими полями:
   * `Id` — уникальный идентификатор;
   * `Title` — название мероприятия;
   * `Description` — описание (может быть пустым);
   * `StartAt` — дата и время начала;
   * `EndAt` — дата и время окончания.

2. **Интерфейс `IEventService`** — определяет контракт для работы с мероприятиями, включая методы:
   * `GetAll(FilterEvent filterEvent, int page, int pageSize)` - получить все мероприятия при пагинации и фильтре;
   * `GetAll(int page, int pageSize)` — получить все мероприятия при пагинации;
   * `GetEventById(Guid id)` — получить мероприятие по ID;
   * `CreateEvent(Event @event)` — создать новое мероприятие;
   * `UpdateEvent(Guid id, Event @event)` — обновить мероприятие;
   * `DeleteEvent(Guid id)` — удалить мероприятие.

3. **RESTful API для управления событиями** — контроллер `EventsController` предоставляет следующие эндпоинты:
   * `GET /events` — список мероприятий с фильтрацией и пагинацией:
     * Параметры фильтрации:
       * `title` (`string`, опциональный) — поиск по названию (регистронезависимый, частичное совпадение);
       * `from` (`DateTime`, опциональный) — события, которые начинаются не раньше указанной даты;
       * `to` (`DateTime`, опциональный) — события, которые заканчиваются не позже указанной даты.
     * Параметры пагинации:
       * `page` (`int`, опциональный, по умолчанию `1`) — номер страницы;
       * `pageSize` (`int`, опциональный, по умолчанию `10`) — количество элементов на странице.
   * `GET /events/{id}` — мероприятие по ID;
   * `POST /events` — создание нового мероприятия;
   * `PUT /events/{id}` — обновление мероприятия;
   * `DELETE /events/{id}` — удаление мероприятия.

4. **Модель `Booking`** — представляет данные о бронировании мероприятия со следующими полями:
   * `Id` — уникальный идентификатор бронирования;
   * `EventId` — идентификатор мероприятия, к которому относится бронирование;
   * `Status` — текущий статус бронирования;
   * `CreatedAt` — дата и время создания бронирования;
   * `ProcessedAt` — дата и время обработки бронирования.

5. **Модель `BookingStatus`** — перечисление, определяющее возможные состояния бронирования на протяжении его жизненного цикла:
   * `Pending` — бронирование ожидает подтверждения (создано, но ещё не обработано);
   * `Confirmed` — бронирование подтверждено системой или администратором;
   * `Rejected` — бронирование отклонено.

6. **Интерфейс `IBookingService`** — определяет контракт для работы с бронированиями, включая методы:
   * `CreateBookingAsync(Guid eventId)` — создаёт новое бронирование для указанного мероприятия;
   * `GetBookingByIdAsync(Guid bookingId)` — получает информацию о бронировании по его уникальному идентификатору;
   * `UpdateProcessedBookingByIdAsync(Guid bookingId, BookingProcessedDto booking)` — обновляет данные обработанного бронирования по его идентификатору.

7. **RESTful API для управления бронированиями** — контроллер `BookingsController` предоставляет следующие эндпоинты:
   * `POST /events/{eventId:Guid}/book` — создание нового бронирования для указанного мероприятия:
     * **Параметры**:
       * `eventId` (`Guid`) — уникальный идентификатор мероприятия, для которого создаётся бронирование.
   * `GET /bookings/{index:Guid}` — получение информации о бронировании по его уникальному идентификатору:
     * **Параметры**:
       * `index` (`Guid`) — уникальный идентификатор бронирования, которое необходимо получить.

8. **Очередь задач бронирования (`IBookingTaskQueue`)**:
   * Потокобезопасная реализация `InMemoryBookingTaskQueue` на основе `ConcurrentQueue<T>`;
   * Методы: `Enqueue(Booking task)` и `TryDequeue(out Booking? task)`.

9. **Background Service для обработки бронирований (`BookingBackgroundService`)**

    **Краткая логика работы:**
    1. Постоянно опрашивает очередь `_bookingTaskQueue` на наличие новых задач.
    2. При обнаружении задачи:
       * логирует начало обработки;
       * имитирует обработку задержкой 2 секунды (`Task.Delay`);
       * обновляет статус бронирования на `Confirmed` и фиксирует время в `ProcessedAt`;
       * логирует успешное завершение обработки.
    3. Корректно останавливается по сигналу отмены (`CancellationToken`).

10. **Интеграция Swagger/OpenAPI** — автоматическая генерация интерактивной документации API

11. **Описание для Swagger** — все эндпоинты имеют подробные комментарии:
    * описания методов;
    * примеры запросов и ответов;
    * коды HTTP‑статусов;
    * параметры и их типы.

12. **Глобальная обработка ошибок** — реализован middleware `GlobalExceptionHandlingMiddleware` для перехвата исключений и возврата структурированного ответа в формате **Problem Details (RFC 7807)**, содержащего:
   * код статуса HTTP;
   * сообщение об ошибке;
   * детали ошибки (при наличии).

13. **Валидация конфигурации**:
    * `ValidateScopes` — проверка корректности внедрения зависимостей (DI) при запуске приложения;
    * `ValidateOnBuild` — проверка конфигурации сервисов при сборке приложения.

14. **Юнит‑тесты** — созданы отдельные тестовые проекты:
    * `EventServiceTest` — тесты для событий (успешные и неуспешные сценарии);
    * `UnitValidatorTest` — тесты валидации дат;
    * `UnitBookingServiceTest` — тесты для сервиса бронирований:
      * успешные сценарии (создание, получение, обновление, обработка бронирований);
      * неуспешные сценарии (некорректные ID, бронирование для несуществующего мероприятия и т. д.).

## Требования

* .NET 10.0 или выше;
* текстовый редактор или IDE (например, Visual Studio, VS Code);
* браузер для доступа к Swagger UI.

## Запуск приложения

1. Клонируйте репозиторий:
```bash
git clone https://github.com/statusak/super-octo-succotash.git
cd super-octo-succotash
```

2. Восстановите зависимости:
```bash
dotnet restore
```

3. Запустите приложение:
```bash
dotnet run
```

## Запуск Unit тестов
```bash
dotnet test
```

## Структура проекта
```txt
├── CSCourse/                 # Основной проект приложения
│   ├── Controllers/          # Контроллеры API
│   ├── Middlewares/          # Middleware-компоненты
│   ├── Models/               # Модели данных
│   ├── Properties/           # Настройки запуска приложения
│   ├── Interfaces/           # Бизнес‑логика: интерфейсы
│   └── Services/             # Бизнес‑логика: реализации сервисов
└── EventServiceTest/         # Проект с юнит‑тестами
```

---

**Примечания по функциональности**:
* Созданные бронирования помещаются в очередь задач (`IBookingTaskQueue`) для последующей обработки.
* Эндпоинты для бронирований интегрированы с Swagger и имеют полную документацию.
* Все ошибки обрабатываются глобально и возвращаются в стандартизированном формате (RFC 7807).

---

**Пример сценария использования (полный цикл — от создания мероприятия до подтверждения бронирования):**

**Шаг 1. Создание мероприятия**
* Пользователь отправляет запрос:
```http
POST /events
{
  "title": "Конференция разработчиков",
  "description": "Ежегодная встреча IT‑специалистов",
  "startAt": "2025-12-10T10:00:00",
  "endAt": "2025-12-10T18:00:00"
}
```

* Система создаёт мероприятие и возвращает его данные с уникальным Id (например, `308dd020-a855-4e80-b29e-b3582b6de65c`).

**Шаг 2. Создание бронирования**
* Пользователь отправляет запрос на бронирование для созданного мероприятия:

```http
POST /events/308dd020-a855-4e80-b29e-b3582b6de65c/book
```

**Сервис:**
*проверяет существование мероприятия `308dd020-a855-4e80-b29e-b3582b6de65c`;
*создаёт запись о бронировании со статусом Pending;
*добавляет задачу в очередь _bookingTaskQueue;
*возвращает ответ 202 Accepted с URL для отслеживания (например `/bookings/2fad1bc1-bece-4c6a-8d84-d99eaf53d5bd`).

**Шаг 3. Проверка статуса бронирования**

*Пользователь запрашивает информацию о бронировании:

```http
GET /bookings/2fad1bc1-bece-4c6a-8d84-d99eaf53d5bd
```
**Сервис  возвращает данные бронирования:**
```json
{
  "id": "2fad1bc1-bece-4c6a-8d84-d99eaf53d5bd",
  "eventId": "308dd020-a855-4e80-b29e-b3582b6de65c",
  "status": "Confirmed",
  "createdAt": "2025-12-09T15:30:00",
  "processedAt": "2025-12-09T15:30:02"
}
```